"""
day12 이지현스그 과제

[Day12] 루프 제어 구조-심화
📌 문제: “가장 느린 API 엔드포인트 찾기” (로그 수집기)

FastAPI 서버를 운영 중이라고 가정하고, 요청 로그를 콘솔에서 수집한다.
여러 API 요청의 경로(path) 와 응답시간(ms) 를 입력받아,
가장 응답이 느렸던 API 경로를 출력하시오.
________________________________________
📥 입력 방식
반복해서 아래 2개를 입력한다.
1.	API 경로 입력 (예: /users, /login, /items/123)
2.	응답시간(ms) 입력 (정수)
단, API 경로에 STOP을 입력하면 입력 종료한다.
(stop, Stop도 인정되게 처리하면 가산점✨)
________________________________________
✅ 검증/전처리 규칙
[경로 입력]
•	strip() 적용 (양쪽 공백 제거)
•	빈 문자열이면 "EMPTY PATH" 출력 후 다시 경로 입력 (continue 느낌)
[응답시간 입력]
•	정수로 변환할 수 없으면 "INVALID TIME" 출력 후 다시 응답시간 입력
•	0 미만이면 "NEGATIVE TIME" 출력 후 다시 응답시간 입력
________________________________________
📌 처리 조건
•	입력이 끝나면, 가장 느린 응답시간을 가진 API 경로와 시간(ms) 를 출력한다.
•	단, 유효한 로그가 1개도 없으면 "NO LOGS" 출력
________________________________________
📤 출력 형식 (고정)
유효 로그가 있을 때:
SLOWEST: {path} ({time}ms)
유효 로그가 없을 때:
NO LOGS
________________________________________
🔥 구현 요구(필수)
•	while True 사용
•	break로 종료
•	continue 사용 (빈 경로 스킵)
•	중첩 루프 사용 (응답시간 유효할 때까지 재입력 받기)
•	리스트 사용 (예: logs에 (path, time) 저장)
•	최댓값 갱신 시 path도 같이 갱신
________________________________________
💡 예시 입력/출력 (참고)
입력:
/login
120
/health
abc
30
/items
-5
50
STOP
중간 출력(검증 메시지):
INVALID TIMENEGATIVE TIME
최종 출력:
SLOWEST: /login (120ms)


"""
